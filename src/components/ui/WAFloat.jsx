/**
 * WAFloat — DEPRECATED
 * Replaced by IGFloat.jsx as the primary floating CTA.
 * WhatsApp buttons are now demo-only and redirect to Instagram.
 * This file is kept for reference only and not rendered anywhere.
 */
export default function WAFloat() {
  return null;
}
